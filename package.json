{
  "name": "ts-auto-school",
  "version": "1.0.0",
  "author": "mchetver",
  "license": "ISC",
  "scripts": {
    "test": "cypress/tests/locked_out_user.spec.ts",
    "cypress:open": "cypress open",
    "cypress:runHeadlessMode": "cypress run",
    "cypress:runHeadlessModeCertain": "cypress run --spec cypress/tests/locked_out_user.spec.js",
    "cypress:runHeadlessChrome": "cypress run --browser chrome --spec cypress/tests/locked_out_user.spec.js"
  },
  "devDependencies": {
    "cypress": "^9.6.0"
  }
}
